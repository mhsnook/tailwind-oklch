# Project Instructions

## Package Manager

Always use `pnpm` instead of `npm` or `yarn` for installing dependencies, running scripts, etc.

## Plans & Documentation

Put plans in `readme/YYYY-MM-plan-name.md` (e.g. `readme/2026-03-luminance-redesign.md`). Do not use a top-level `plan.md`.
